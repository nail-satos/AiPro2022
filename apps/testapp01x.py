# 必要なライブラリをインポートする
import streamlit as st

# testapp01(課題2)
def app():
    st.title('Streamlitの標準UIを試そう')
    st.caption('初級(必須)：1～2種類のUIを実装した')
    st.caption('中級(任意)：3～5種類のUIを実装した')
    st.caption('上級(任意)：6種類以上のUIを実装した')

    st.subheader('課題の進め方')
    st.write('Streamlitの公式ドキュメント「API reference」')
    st.write('https://docs.streamlit.io/library/api-reference')
    st.write('から、好きなUIをクリックする → コードの記述例(Example)をコピーする → 動作確認')

# 課題2. Streamlitの標準UIを試そう
# 初級（必須）：1～2種類のUIを実装した
# 中級（任意）：3～5種類のUIを実装した
# 上級（任意）：6種類以上のUIを実装した
#-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-#
# Streamlitの公式ドキュメント「API reference」
# https://docs.streamlit.io/library/api-reference
# から、好きなUIをクリックする → コードの記述例(Example)をコピーする → 動作確認
